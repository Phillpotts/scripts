/**
 * The sum of passed numbers
 * @param a first number to be added
 * @param b second number to be added
 * @returns the sum of the numbers
 */
export const sum = (a: number, b: number): number => {
    return a + b
}
